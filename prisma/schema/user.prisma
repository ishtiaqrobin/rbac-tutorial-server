// user.prisma is now REMOVED — the User model lives in auth.prisma
// because Better-Auth needs User, Session, Account, Verification in the same file / schema.
//
// The old custom `Session` model is also replaced by Better-Auth's Session in auth.prisma.
